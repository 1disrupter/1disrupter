[build]
builder = "NIXPACKS"
buildCommand = "pip install -r backend/requirements.txt"

[deploy]
startCommand = "uvicorn backend.server:app --host 0.0.0.0 --port ${PORT:-8000}"
rootDirectory = "backend"
