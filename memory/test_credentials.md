# Vibe2Nite — Test credentials

## Admin Console (`/admin`)
Simple local-only auth (localStorage-based).

| Field | Value |
|---|---|
| Username | `vibe2nite` |
| Password | `nightowl` |

The hint is visible on the login screen. When production auth is added, replace this with real backend credentials.

## Backend endpoints
No auth required yet on any endpoint. All endpoints under `/api/*`.
